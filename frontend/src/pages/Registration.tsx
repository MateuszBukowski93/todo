import React from "react";
import Layout from "../components/layout/Layout";

interface IRegistration {
  path: string;
}
const Registration = ({ path }: IRegistration) => {
  return <Layout>Registration</Layout>;
};

export default Registration;
